<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HRApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryVariant">@color/DarkBackground</item>
        <item name="colorOnPrimary">@color/DarkText</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/DarkText</item>
        <item name="colorSecondaryVariant">@color/DarkBackground</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:title">ECV Crew</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/DarkText</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.myTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryVariant">@color/DarkBackground</item>
        <item name="colorOnPrimary">@color/DarkText</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/DarkText</item>
        <item name="colorSecondaryVariant">@color/DarkBackground</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/DarkText</item>
    </style>
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/DarkBackground</item>
        <item name="android:textColorSecondary">@color/DarkBackground</item>
        <item name="actionMenuTextColor">@color/DarkBackground</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>
    <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/DarkBackground</item>
    </style>
</resources>